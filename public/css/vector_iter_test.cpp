// Create
vector< vector<int>> vec(4, vector<int(4)>;
// Write
vec[2][3] = 10;
// Read
int a = vec[2][3];
//insert
use push_back or insert

// You could also use iterators:

    for (vector< vector<int> >::iterator it = vec.begin(); it != vec.end(); ++it) {
        it->push_back(j); // Add column to all rows		
}